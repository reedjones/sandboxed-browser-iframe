import React from 'react';
import { Separator as AriaSeparator } from 'react-aria-components';

const Separator = ({ orientation }) => {
    return <AriaSeparator orientation={orientation} />;
};

export default Separator;
